<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >
    <TextView
        android:id="@+id/show_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="#000"
        android:textSize="20sp"
        android:text="@string/searchOrInput"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_toLeftOf="@+id/button_refresh"
        android:layout_centerVertical="true"
        />
    <TextView
        android:layout_centerVertical="true"
        android:id="@+id/mult_window_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/ic_crop_din_black_24dp"
        android:layout_alignParentRight="true"
        android:gravity="center"
        />
<TextView
    android:layout_centerVertical="true"
    android:background="@drawable/ic_refresh_black_24dp"
    android:id="@id/button_refresh"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/mult_window_number"
    android:layout_marginRight="10dp"
    />

</RelativeLayout>
